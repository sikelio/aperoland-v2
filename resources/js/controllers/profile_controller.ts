import { Controller } from '@hotwired/stimulus';

export default class extends Controller {
  public updateUsernameMail(e: Event) {
    e.preventDefault();
  }

  public updatePassword(e: Event) {
    e.preventDefault();
  }

  public deleteAccount(e: Event) {
    e.preventDefault();
  }
}
