enum ValidationMessages {
  MIN_LENGTH = 'minLength validation failed',
  MAX_LENGTH = 'maxLength validation failed',
  REQUIRED = 'required validation failed',
  UNIQUE = 'unique validation failure',
}

export default ValidationMessages;
